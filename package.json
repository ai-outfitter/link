{
  "name": "@ai-outfitter/link",
  "version": "1.6.0",
  "description": "Audit organizations against the Outfitter SDLC governance baseline: a read-only scanner that rates repositories on the agentic adoption ramp.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ai-outfitter/link.git"
  },
  "homepage": "https://github.com/ai-outfitter/link#readme",
  "bugs": "https://github.com/ai-outfitter/link/issues",
  "keywords": [
    "ai-outfitter",
    "sdlc",
    "agents",
    "governance",
    "report"
  ],
  "bin": {
    "link": "dist/cli.js"
  },
  "exports": {
    ".": "./dist/cli.js",
    "./schema": {
      "types": "./dist/core/schema.d.ts",
      "default": "./dist/core/schema.js"
    },
    "./octokit": {
      "types": "./dist/core/octokit.d.ts",
      "default": "./dist/core/octokit.js"
    },
    "./core": {
      "types": "./dist/core/core.d.ts",
      "default": "./dist/core/core.js"
    },
    "./testing": {
      "types": "./dist/core/testing.d.ts",
      "default": "./dist/core/testing.js"
    }
  },
  "files": [
    "dist",
    "dist-web",
    "agents",
    "environments",
    "governance",
    "spec",
    "workflows",
    "docs",
    "README.md"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.core.json",
    "build:web": "node scripts/build-web.mjs",
    "prepare": "npm run build",
    "prepack": "npm run build && npm run build:web",
    "typecheck": "tsc -p tsconfig.build.json --noEmit && tsc -p tsconfig.core.json --noEmit",
    "report": "node dist/cli.js report",
    "test:evidence": "node scripts/test-evidence.mjs",
    "test:sources": "node scripts/test-sources.mjs"
    ,"test:reviews": "node scripts/test-reviews.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
